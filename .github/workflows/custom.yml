name: Publish and Deploy Docker Image

on:
  push:
    branches: ['dev', 'deploy']

jobs:
  # Build and Push Docker Image
  build_and_push:
    runs-on: [self-hosted, runner-on-server]
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: ${{ secrets.DOCKER_HUB_USERNAME }}/one

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/one:latest
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    name: Deploy Docker image
    runs-on: [self-hosted, runner-on-server]
    needs: build_and_push
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

#      - name: Set up Docker Compose
#        run: |
#          sudo apt-get update
#          sudo apt-get install -y docker-compose

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Copy .env file
        run: |
          cp .env.example .env

      - name: Deploy with Docker Compose
        run: |
          ls -a
          docker-compose -f docker-compose.yml up -d --build

      - name: Clean up unused Docker images
        run: |
          docker image prune -f


#  deploy:
#    needs: build_and_push
#    runs-on: self-hosted
#    steps:
#      - name: Check out the repo
#        uses: actions/checkout@v3
#
#      - name: Set up SSH
#        uses: webfactory/ssh-agent@v0.7.0
#        with:
#          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
#
#      - name: Deploy application via SSH
#        run: |
#          ssh savvycom@10.22.4.168 -p 22 << 'EOF'
#            cd /home/savvycom/one-github
#            docker-compose up -d
#          EOF